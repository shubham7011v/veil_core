rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: These rules allow anyone with your project ID to read/write.
      // You should restrict this to authenticated users once the app is ready.
      allow read, write: if request.auth != null;
    }
  }
}
